using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PerkSpawn : MonoBehaviour
{
    // spawn perks (health, energy, bullets) on random positions
    private string healthTag = "HealthRefill", energyTag = "EnergyRefill", bulletTag = "BulletRefill";
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
